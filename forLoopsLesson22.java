package zeq.from.beginning;

public class forLoopsLesson22 {
    public static void main(String[] args) {

        // c1 = steps to count= c2 = up to what nr and c3 how much to increment!
        for (int counter=1;counter<=10;counter++){   // if u want to step more than 1 = counter+=3
            System.out.println(counter);
        }
    }
}
